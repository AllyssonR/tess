function contar(){
    var num_inicio = document.querySelector('')
    var num_final = document.querySelector('')
    var inicio = Number(num_inicio)
    var final = Number(num_final)
    for(inicio;inicio<=final;inicio++){

    }
}